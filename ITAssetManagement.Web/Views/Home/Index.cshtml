@model ITAssetManagement.Web.Extensions.PaginatedList<ITAssetManagement.Web.Models.Laptop>

@{
    ViewData["Title"] = "Home Page";
    var prevDisabled = !Model.HasPreviousPage ? "disabled" : "";
    var nextDisabled = !Model.HasNextPage ? "disabled" : "";
}

<div class="text-center">
    <h1 class="display-4">Welcome to IT Asset Management</h1>
    <p>A comprehensive solution for managing IT assets in your organization.</p>
</div>

<div class="row">
    <div class="col-md-12">
        <h2>Latest Assets</h2>
        <hr />
        <!-- List of laptops will go here -->
        @if (Model?.Count > 0)
        {
            <div class="table-responsive">
                <table class="table table-striped table-hover">
                    <thead>
                        <tr>
                            <th>Tag Number</th>
                            <th>Brand</th>
                            <th>Model</th>
                            <th>Status</th>
                            <th>Registration Date</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var laptop in Model!)
                        {
                            <tr>
                                <td>@laptop.EtiketNo</td>
                                <td>@laptop.Marka</td>
                                <td>@laptop.Model</td>
                                <td>@laptop.Durum</td>
                                <td>@laptop.KayitTarihi.ToShortDateString()</td>
                                <td>
                                    <a class="btn btn-sm btn-info" asp-controller="Laptops" asp-action="Details" asp-route-id="@laptop.Id">Details</a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>

            <nav aria-label="Page navigation" class="mt-4">
                <div class="d-flex justify-content-between align-items-center">
                    <div class="text-muted">
                        Sayfa @Model.PageIndex / @Model.TotalPages (@Model.TotalItems toplam kayıt)
                    </div>
                    <ul class="pagination mb-0">
                        <li class="page-item @prevDisabled">
                            <a class="page-link" asp-action="Index" asp-route-page="@(Model.PageIndex - 1)">
                                <i class="fas fa-chevron-left"></i>
                            </a>
                        </li>
                        @for (int i = 1; i <= Model.TotalPages; i++)
                        {
                            <li class="page-item @(i == Model.PageIndex ? "active" : "")">
                                <a class="page-link" asp-action="Index" asp-route-page="@i">@i</a>
                            </li>
                        }
                        <li class="page-item @nextDisabled">
                            <a class="page-link" asp-action="Index" asp-route-page="@(Model.PageIndex + 1)">
                                <i class="fas fa-chevron-right"></i>
                            </a>
                        </li>
                    </ul>
                </div>
            </nav>
        }
        else
        {
            <p>No assets found in the system.</p>
        }
    </div>
</div>
