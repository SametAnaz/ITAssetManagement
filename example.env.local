# Rename this folder as .env.local
# Fill the variables for your environment

DB_CONNECTION_STRING=server=<server ip>;port=<server port>;database=<Database Name>;user=<db user>;password=<user password>;

# Authentication Credentials
# Fill as you want
USER_ACCOUNT=<your admin account name>
USER_PASSWORD=<your password>
